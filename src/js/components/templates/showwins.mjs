export function showHistoryWins(ele) {
    console.log('hei');
    for (let i = 0; i < ele.wins.length; i++) {
        console.log(ele.wins.length);
        console.log(ele);
        if (ele.wins !== 0) {
            console.log('object');
        } else {
            console.log('hei');
        }
    }
}
